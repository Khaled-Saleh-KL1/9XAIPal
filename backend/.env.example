# 9XAIPal Backend - Local Development
# Copy this file to .env and fill in your own values.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=9xaipal
POSTGRES_USER=9xaipal
POSTGRES_PASSWORD=9xaipal_dev_password
STORAGE_ROOT=app/storage

# ── Host ports for `docker compose up` ──────────────────────────────────────
# Only the HOST side of each mapping. Containers always reach each other by
# service name on the compose network (postgres:5432, redis:6379), so changing
# these cannot break the stack — they exist for when another project on the
# same machine already holds the default.
API_PORT=8000        # the whole app: UI at / and API at /api/v1
REDIS_PORT=6379

# ── Ollama (local or remote/cloud backend) ──────────────────────────────────
# These models are used whenever Ollama is the active backend.
OLLAMA_BASE_URL=http://localhost:11434
# Optional API key for hosted/protected Ollama endpoints (e.g. Ollama Cloud).
# Leave empty for a local instance — it stays keyless.
OLLAMA_API_KEY=
CHAT_MODEL=
# Vision model for figure descriptions / image questions. Empty = reuse CHAT_MODEL.
VLM_MODEL=
# e.g. qwen3-embedding:0.6b — pull it first with `ollama pull qwen3-embedding:0.6b`.
EMBEDDING_MODEL=
# Max characters of a chunk sent to the embedder (Ollama 400s on over-window
# inputs; ~3000 is safe locally — cloud embedders can take much more).
EMBED_MAX_CHARS=3000
# Stored embedding dimension. Larger model outputs are truncated+renormalized
# (MRL), smaller ones zero-padded. Keep ≤ 2000 so the pgvector HNSW index can
# exist (default 1024). Changing this re-embeds the whole library on next start.
VECTOR_DIMENSION=1024
DEBUG=true

# ── AI backend selection (local Ollama or cloud APIs) ─────────────────────
# auto (default) = use Ollama when it is reachable at OLLAMA_BASE_URL;
# otherwise fall back to the first cloud API key below, in this order:
# OpenAI → Anthropic → xAI → DeepSeek. If neither Ollama nor a key
# is available, the app answers with clear instructions to add one.
# Pin a backend explicitly with: ollama | openai | anthropic | xai |
# deepseek | custom (any OpenAI-compatible endpoint via LLM_BASE_URL).
LLM_PROVIDER=auto
# Generic key for a pinned LLM_PROVIDER; the per-provider keys below also work.
LLM_API_KEY=
# Only needed for LLM_PROVIDER=custom (e.g. OpenRouter, vLLM, llama.cpp
# server) or to override a provider's default URL.
LLM_BASE_URL=

# Per-provider API keys — paste any you have; in auto mode the first one
# found (in the order above) is used when Ollama is off.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
# Not active by default — held here as a template until we're ready to wire
# it in via LLM_PROVIDER=deepseek.
DEEPSEEK_API_KEY=

# Models used when a cloud provider is active (defaults shown — uncomment to
# change). CHAT_MODEL / VLM_MODEL / CLASSIFIER_MODEL above stay reserved for
# Ollama (and "custom"), so an Ollama tag is never sent to a cloud API.
# OPENAI_CHAT_MODEL=gpt-4o
# ANTHROPIC_CHAT_MODEL=claude-sonnet-4-6
# XAI_CHAT_MODEL=grok-4
# DEEPSEEK_CHAT_MODEL=deepseek-chat   # no vision: figure images can't be described
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Turn on reasoning/thinking mode for OpenAI o-series models (o1, o3-mini,
# o4-mini, etc.) and custom OpenAI-compatible endpoints that support it.
# Ignored for Anthropic, xAI, DeepSeek, and Ollama.
CLOUD_THINKING_MODE=false

# Embeddings follow the same auto chain, but only OpenAI offers an embedding
# API among the cloud providers above (Anthropic/xAI/DeepSeek have none).
# Pin it (e.g. EMBEDDING_PROVIDER=openai) when switching to cloud
# permanently — stale vectors are then wiped and the library re-embeds
# automatically on startup. Key/base-url fall back to LLM_API_KEY /
# LLM_BASE_URL when empty.
EMBEDDING_PROVIDER=auto
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=

# ── Paper-only mode ───────────────────────────────────────────────────────
# Skip the embedding pass for documents small enough to fit whole in the chat
# model's context window, and answer GLOBAL questions from full-text search
# instead of pgvector. See docs/plans/paper-only-embedding-skip.md.
#
# Default false so upgrading changes nothing. The decision is made once, at
# ingestion, and stored in documents.embedding_mode — changing this setting (or
# the threshold) never reclassifies documents that are already ingested.
#
# ⚠ Only segment S1 of the plan has landed. With this on today, a skipped
# document's GLOBAL route degrades to full-text search alone; the
# document-stuffing fallback for questions FTS cannot answer arrives in S2.
PAPER_ONLY_MODE=false
# Skip only when SUM(chunks.token_count) is at or below this. Deliberately far
# below the model's full window: the body shares that window with the system
# prompt, conversation history, and the answer. token_count is a len/4
# heuristic that undercounts math and tables, so leave headroom.
PAPER_ONLY_MAX_TOKENS=50000

# ── Web search ────────────────────────────────────────────────────────────
# Provider(s) for the EXTERNAL chat route, the research agent, and the paper
# agent's WEB tool. auto (default) tries every configured key below in
# order — google, tavily, linkup, exa, serpapi, duckduckgo — and falls
# through to the next the moment one errors or comes back empty. Pin to one
# name to force it alone with no fallback (debugging only), or "none" to
# disable web search entirely (duckduckgo needs no key, so leaving every
# other one blank still leaves web search on — "none" is the only way off).
#
# ⚠ The first five are hosted third parties: the QUERY STRING leaves this
# machine for whichever one answers. duckduckgo scrapes directly (no
# official API), but the query still leaves the machine either way. Paper
# text, chunks, and chat history never do, on any of the six.
WEB_SEARCH_PROVIDER=auto
# Get one at https://app.tavily.com — the free tier is 1,000 searches/month
# PER KEY. ⚠ This accepts a COMMA-SEPARATED LIST: the client rotates through
# them, so when one key's allowance runs out it moves to the next and only
# gives up (falling through to Linkup) once every key is spent. More free
# headroom is just more keys — no paid plan, no code change.
#   TAVILY_API_KEY=tvly-dev-aaa,tvly-dev-bbb,tvly-dev-ccc
TAVILY_API_KEY=
# basic (1 credit, fast) | advanced (2 credits, deeper extraction)
TAVILY_SEARCH_DEPTH=basic
# Get one at https://app.linkup.so
LINKUP_API_KEY=
# Get one at https://exa.ai — no image-search endpoint, text results only.
EXA_API_KEY=
# Get one at https://serpapi.com — genuine Google SERP data (text + images).
SERPAPI_API_KEY=
# DuckDuckGo needs no key at all — scraped directly via the `ddgs` library.
# Always on, always last: the one provider that can't run out of quota or
# have a bad key, so it's what's left if every key above is empty or every
# paid provider above is down.

# ── Security ──────────────────────────────────────────────────────────────
# Browser origins allowed to call the API (comma-separated). Add your LAN
# address (e.g. http://192.168.1.50:5173) when other machines use the dev UI.
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
# Per-client-IP request ceiling per minute across /api routes. 0 disables.
RATE_LIMIT_PER_MINUTE=300

# ── Capacity ───────────────────────────────────────────────────────────────
# Signup is open — anyone can create an account, no invite code. This is
# what actually protects a single box with no autoscaling if a burst of
# people ever showed up at once: admit up to this many CONCURRENTLY ACTIVE
# users (made a request in the last ACTIVE_WINDOW_SECONDS — not "has a
# session", which lasts 30 days), queue everyone past that, auto-promote the
# moment a slot frees. See app/core/capacity.py. Not expected to matter at
# normal usage levels.
MAX_ACTIVE_USERS=30
ACTIVE_WINDOW_SECONDS=300
# Same idea for uploads: this box's Celery worker processes one document at
# a time (--concurrency=1), so this caps how many can be queued/in-progress
# at once rather than letting an extreme burst grow disk/DB rows unbounded.
# A fresh upload past this is rejected with a clear error instead of queuing
# forever. See app/services/ingestion.py::check_queue_capacity.
MAX_QUEUED_INGESTION_JOBS=50

# ── Latency tuning ────────────────────────────────────────────────────────
# Small model (e.g. llama3.2:3b) for router/guardrail classification — the
# single biggest /ask speedup. Empty = reuse CHAT_MODEL.
CLASSIFIER_MODEL=
# Keep models resident in memory between requests ("30m", "-1" = forever).
OLLAMA_KEEP_ALIVE=30m
# Max concurrent /ask requests per uvicorn worker (protects Ollama from OOM).
MAX_CONCURRENT_ASKS=3

# Ollama runtime - use this when starting ollama:
# OLLAMA_FLASH_ATTENTION=0 ollama serve
# OLLAMA_FLASH_ATTENTION=0

# HuggingFace auth — used by MinerU to download model weights from HF Hub.
# huggingface_hub reads HUGGING_FACE_HUB_TOKEN / HF_TOKEN automatically.
HF_TOKEN=
HUGGING_FACE_HUB_TOKEN=
